import { TopPlayerHome } from './TopPlayerHome.model';

export class TopHome {
    day: Array<TopPlayerHome> = new Array<TopPlayerHome>();
    month: Array<TopPlayerHome> = new Array<TopPlayerHome>();
}